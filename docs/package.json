{
  "name": "@skysend/docs",
  "version": "2.5.1",
  "private": true,
  "scripts": {
    "dev": "vitepress dev",
    "build": "git fetch --unshallow 2>/dev/null || true && vitepress build",
    "build:local": "vitepress build",
    "preview": "vitepress preview"
  },
  "devDependencies": {
    "vitepress": "^1.6.0",
    "vue": "^3.5.0"
  }
}
